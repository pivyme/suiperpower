{
  "name": "dusdc-faucet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-serve DUSDC faucet for the DeepBook Predict testnet.",
  "scripts": {
    "deploy": "bun run scripts/deploy.ts",
    "deploy:check": "bun run scripts/deploy.ts --dry-run --which=test",
    "typecheck:scripts": "tsc -p scripts/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^1.40.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
