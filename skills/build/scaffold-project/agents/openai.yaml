name: scaffold-project
version: 1
phase: build
description: |
  Scaffold a new Sui project with the right stack, write the initial
  .suiperpower/build-context.md, and pick template defaults.
  Use when the user says "scaffold my project", "set up my workspace",
  "what stack should I use on Sui", "init a Sui project", "bootstrap a Sui app",
  "start a new Sui project", or "give me a Sui template".
  Reads .suiperpower/idea-context.md if present.
prompt_path: ../SKILL.md
references:
  - ../references/template-shapes.md
  - ../references/move-toml-defaults.md
knowledge:
  - skills/data/sui-knowledge/04-protocols-and-sdks.md
