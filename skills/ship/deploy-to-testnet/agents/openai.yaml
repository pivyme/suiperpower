name: deploy-to-testnet
version: 1
phase: ship
description: |
  Publish the user's Move package to Sui testnet using the canonical deploy
  runbook, capture the package id and upgrade cap, and write
  deploy-context.md so downstream skills can reference the real artifacts.
  Use when the user says "deploy to testnet", "publish to Sui testnet",
  "publish my package to testnet", "ship to testnet", "deploy my contract to
  testnet", or "publish move package testnet".
prompt_path: ../SKILL.md
references:
  - ../references/preflight-checklist.md
  - ../references/troubleshooting.md
